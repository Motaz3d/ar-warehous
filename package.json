{
  "name": "site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
  "build": "cp -r ./css ./www/css && cp -r ./js ./www/js && cp index.html ./www/index.html && npx cap copy android && npx cap sync android",
  "android": "npx cap open android"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^7.1.0",
    "@capacitor/cli": "^7.1.0",
    "@capacitor/core": "^7.1.0"
  }
}
